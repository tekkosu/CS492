import 'package:flutter/material.dart';

class AlphaScreen extends StatelessWidget {
  final String title = 'Alpha';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Alpha')
    );
  }
}